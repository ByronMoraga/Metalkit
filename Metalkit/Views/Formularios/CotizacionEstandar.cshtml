@model Metalkit.Models.Cotizacion
@using Metalkit.Models;

@{
    ViewBag.Title = "Edit";
    Layout = null;
    List<Producto> detalleProducto = new List<Producto>();
    detalleProducto = ViewBag.detalleProducto;
    List<Parametro> param = ViewBag.param;
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="col-lg-12">
        <div class="ibox ">
            <div class="ibox-title">
                <h5>
                    Detalle de proyecto Estándar
                </h5>
                <div class="ibox-tools">
                    <a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                </div>
            </div>
            <div class="ibox-content">
                <div class="panel-body">

                    <div class="row">
                        <div class="col-md-4 form-group">
                            <label for="itramite">Modelo a cotizar</label>
                            @{

                                @Html.DropDownList("iproyecto", null, "Seleccione", htmlAttributes: new { @class = "form-control fondo search-select", @onchange = "CargarProyecto()" })

                            }
                        </div>
                    </div>
                    <div class="row">
                        @*@{
                            if (detalleProducto.Count > 0)
                            {*@
                                <table class="table table-striped table-hover tablaSecundaria dataTable no-footer" width="100%" id="tabla_Producto">
                                    <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Codigo</th>
                                            <th>Descripcion</th>
                                            <th>Superficie</th>
                                            <th>Precio</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @*@{
                                            foreach (var item in detalleProducto)
                                            {
                                                <tr>
                                                    <td>@item.Id</td>
                                                    <td>@item.Codigo</td>
                                                    <td>@item.Descripcion</td>
                                                    <td>@item.Superficie</td>
                                                    <td>@item.Precio</td>
                                                </tr>
                                            }
                                        }*@
                                    </tbody>
                                </table>
                            @*}
                        }*@
                    </div>
                    <hr />
                    <div class="row">
                        <table class="table table-striped table-hover tablaSecundaria dataTable no-footer" width="100%" id="tabla_parametro">
                            <thead>
                                <tr>
                                    <th></th>
                                    <th>Descripcion</th>
                                    <th>Superficie</th>
                                    <th>Precio</th>
                                </tr>

                            </thead>
                            <tbody>
                        
                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>
    </div>
    <div class="form-horizontal">
        <h4>Despacho</h4>
        <hr />
        <div class="ibox ">
            <div class="ibox-title">
                <h5>Datos del cliente</h5>
                <div class="ibox-tools">
                    <a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                </div>
            </div>
            <div class="ibox-content">
                <div class="panel-body">
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label">Fecha Entrega</label>
                        <div class="col-lg-10">
                            <div class="input-group date col-md-5">
                                <span class="input-group-addon"><i class="fa fa-calendar"></i></span><input type="text" class="form-control" value="03/04/2014">
                            </div>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label">Tipo Entrega</label>
                        <div class="col-lg-10">
                            <input type="text" placeholder="Tipo Entrega" class="form-control" id="tbTipoEntrega" name="tbTipoEntrega">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label">Direccion</label>
                        <div class="col-lg-10">
                            <input type="text" placeholder="Direccion" class="form-control" id="tbDireccion" name="tbDireccion">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label">Comuna</label>
                        <div class="col-lg-10">
                            <input type="text" placeholder="Comuna" class="form-control" id="tbComuna" name="tbComuna">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label">Region</label>
                        <div class="col-lg-10">
                            <input type="text" placeholder="Region" class="form-control" id="tbRegion" name="tbRegion">
                        </div>
                    </div>

                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

<script>
    //Grid_VM.init();
</script>
}
